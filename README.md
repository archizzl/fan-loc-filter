# fan_loc_filter

See `fan_data.csv` for example fan data to upload and play with.

## Get Started

Run these commands from the project root:

```bash
npm install
npm run dev
```

Then open the local URL shown in the terminal (typically `http://localhost:5173`).

## Other Useful Commands

```bash
npm run build
npm run preview
npm run lint
```